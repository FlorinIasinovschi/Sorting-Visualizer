export function sortAlgo(arr) {
  let newArr = [];
  for (let i = 0; i < arr.length; i++) {
    newArr.push(i);
    // if (arr[i] > arr[i + 1]) {
    //   let temp = arr[i + 1];
    //   arr[i + 1] = arr[i];
    //   arr[i] = temp;

    //   while (arr[i] < arr[i - 1]) {
    //     let temp2 = arr[i - 1];
    //     arr[i - 1] = arr[i];
    //     arr[i] = temp2;
    //   }
    // }


  }


  return newArr;
}

